services:
  - type: web
    name: arkgrid-gem-viewer
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LOSTARK_API_KEY
        sync: false
